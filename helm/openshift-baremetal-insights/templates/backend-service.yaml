# helm/openshift-redfish-insights/templates/backend-service.yaml
apiVersion: v1
kind: Service
metadata:
  name: {{ include "redfish-insights.fullname" . }}-backend
  namespace: {{ .Values.namespace.name }}
  labels:
    {{- include "redfish-insights.labels" . | nindent 4 }}
    app.kubernetes.io/component: backend
  annotations:
    service.beta.openshift.io/serving-cert-secret-name: {{ include "redfish-insights.fullname" . }}-backend-cert
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.backend.service.port }}
      targetPort: https
      protocol: TCP
      name: https
  selector:
    {{- include "redfish-insights.backend.selectorLabels" . | nindent 4 }}
